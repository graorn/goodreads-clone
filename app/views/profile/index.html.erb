<h1>Favorite books</h1>

<div class="row">
  <% @favorite_books.each do |favorite_book| %>

    <div class="col col-xs-6 col-md-3 col-lg-2">
      <div class="card mb-5">
        <%= link_to(image_tag(favorite_book.cover, class: 'img-fluid'), favorite_book) %>

        <div class="card-body">
          <h5 class="card-title">  <%= favorite_book.title %> </h5>
          <p> <%= favorite_book.author %> </p>

          <p class="card-text"> <%= truncate(favorite_book.description, length: 100) %> </p>

          <%= link_to 'More', favorite_book, class: 'btn btn-primary center-block' %>

        </div>
      </div>

    </div>
  <% end %>
</div>

<h1>To read: </h1>

<div class="row">
  <% @to_read_books.each do |to_read_book| %>

    <div class="col col-xs-6 col-md-3 col-lg-2">
      <div class="card mb-5">
        <%= link_to(image_tag(to_read_book.cover, class: 'img-fluid'), to_read_book) %>

        <div class="card-body">
          <h5 class="card-title">  <%= to_read_book.title %> </h5>
          <p> <%= to_read_book.author %> </p>

          <p class="card-text"> <%= truncate(to_read_book.description, length: 100) %> </p>

          <%= link_to 'More', to_read_book, class: 'btn btn-primary center-block' %>

        </div>
      </div>

    </div>
  <% end %>
</div>

